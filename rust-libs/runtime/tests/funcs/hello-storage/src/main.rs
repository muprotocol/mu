use musdk::*;

#[mu_functions]
mod hello_storage {
    use super::*;

    //#[mu_function]
    // fn storage_list<'a>(ctx: &'a mut MuContext) -> () {
    //     let sth = ctx.

    //     ()
    // }
}
